import { styled } from '@stitches/react';

const Slider = () =>{
    return (
        <div>
            <input type="range" min="1" max="10" >
            </input>
        </div>
    );
}

export default Slider;